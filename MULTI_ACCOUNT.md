# 多账号稳定运行

版本：0.20.0

## 推荐部署

五个账号应使用五个独立 Chrome/Edge 配置文件，或五个独立指纹浏览器环境。每个环境只登录一个 X 账号，并分别加载本扩展。不要在同一个浏览器配置文件中切换多个账号。

## 稳定性机制

- 自动任务记录启动时的 X 登录账号，恢复时再次校验；账号不一致会暂停，避免串号发送。
- 任务使用标签页租约与定向心跳。正常运行时只有持有租约的标签页推进任务。
- 标签页被替换或浏览器重启后，租约过期可由同账号页面接管。
- 评论和发帖分别设置每小时、每日上限。达到上限时等待下一个时间窗口，不删除任务。
- 可限制账号活跃时段，并设置连续失败熔断次数。

## 建议起始参数

单个账号建议从以下参数开始观察：

- 评论：每小时 20–30，每日 100–200。
- 发帖：每小时 4–8，每日 10–30。
- 活跃时段：按账号目标地区设置 8–16 小时。
- 连续失败熔断：5 次。

五个环境之间应错开启动时间和时间区间。扩展允许设置更高上限，但更高上限不等于平台允许持续高频操作；应根据账号状态逐步调整。
